# Future infrastructure

This directory is reserved for reproducible local and deployment infrastructure. Prompt 01 intentionally adds no containers, databases, caches, or deployment resources.

Planned infrastructure areas:

- Docker Compose orchestration
- database initialization
- Redis configuration
- observability configuration
- deployment manifests
- recovery scripts

Infrastructure changes must use fake local values, keep credentials outside Git, and document safe startup and teardown procedures.
