# Future shared packages

This workspace boundary is reserved for small, deliberately shared packages. It must not become a place for hidden service coupling or shared database access.

Planned package areas:

- contracts
- configuration
- security primitives
- UI components
- testing utilities

Packages will be created only when at least one implemented application or service has a concrete need for them.
